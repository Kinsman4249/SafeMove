# SafeMove
SafeMove is a safety‑first PowerShell script for recursive file migrations that never overwrite destination data. It supports dry‑run (-WhatIf), size‑based filtering, collision‑safe renaming, and sub‑second CSV audit logging for high‑risk or auditable moves.
